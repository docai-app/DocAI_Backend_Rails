<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style type="text/css">
      body {
        font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
        margin: 0;
        padding: 0;
        background-color: #f4f4f4;
        color: #333333;
      }
      .container {
        width: 100%;
        max-width: 600px;
        margin: 0 auto;
        background-color: #ffffff;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0,0,0,0.1);
      }
      .header {
        text-align: center;
        padding-bottom: 20px;
        border-bottom: 1px solid #dddddd;
      }
      .header img {
        max-width: 150px; /* 您可以替換成您的 AI ENGLISH Logo URL */
      }
      .content {
        padding: 20px 0;
        line-height: 1.6;
      }
      .content h1 {
        font-size: 22px;
        color: #333333;
      }
      .content p {
        font-size: 16px;
        margin-bottom: 20px;
      }
      .button-container {
        text-align: center;
        margin: 30px 0;
      }
      .button {
        background-color: #007bff; /* 專業藍色 */
        color: #ffffff;
        padding: 15px 30px;
        text-decoration: none;
        border-radius: 5px;
        font-size: 16px;
        font-weight: bold;
        display: inline-block;
      }
      .footer {
        text-align: center;
        padding-top: 20px;
        border-top: 1px solid #dddddd;
        font-size: 12px;
        color: #777777;
      }
      .footer a {
        color: #007bff;
        text-decoration: none;
      }
      .small-text {
        font-size: 12px;
        color: #777777;
        margin-top: 15px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <%# 替換為您的 Logo 圖片路徑或 URL %>
        <%# link_to image_tag('your_logo.png', alt: 'AI ENGLISH Logo'), root_url %>
        <h2>AI ENGLISH</h2>
      </div>
      <div class="content">
        <h1>Reset Your Password</h1>
        <p>Hello <%= @resource.nickname || @resource.email %>!</p>
        <p>
          Someone (hopefully you!) has requested a link to change your AI ENGLISH account password. You can do this by clicking the button below:
        </p>
        <div class="button-container">
          <%# @token 應該由 Devise::Mailer 自動設置 %>
          <a href="<%= edit_password_url(@resource, reset_password_token: @token) %>" class="button">Change my password</a>
        </div>
        <p>
          This link will expire in <%= Devise.reset_password_within.inspect %>.
        </p>
        <p>
          If you didn't request this, please ignore this email. Your password won't change until you access the link above and create a new one.
        </p>
        <p>Thank you,<br>
          The AI ENGLISH Team</p>
        <p class="small-text">
          If you're having trouble clicking the button, copy and paste the URL below into your web browser:<br>
          <a href="<%= edit_password_url(@resource, reset_password_token: @token) %>"><%= edit_password_url(@resource, reset_password_token: @token) %></a>
        </p>
      </div>
      <div class="footer">
        <p>&copy; <%= Time.now.year %> AI ENGLISH. All rights reserved.</p>
        <%# 您可以添加更多頁腳信息 %>
      </div>
    </div>
  </body>
</html> 